# packages/langchain_compat/Makefile
.PHONY: serve

serve:
	gollum $$(git rev-parse --show-toplevel) \
	       --page-file-dir packages/langchain_compat/wiki \
	       --ref compat-only \
	       --no-edit