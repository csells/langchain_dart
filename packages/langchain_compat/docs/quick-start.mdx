---
title: Quick Start
---

# Quick Start

Get started in 30 seconds.

## Install

```yaml
dependencies:
  dartantic_ai: ^1.0.0
```

## Chat

```dart
final agent = Agent('openai');
final result = await agent.send('Hello!');
print(result.output);
```

## Stream

```dart
await for (final chunk in agent.sendStream('Tell a joke')) {
  stdout.write(chunk.output);
}
```

## Embeddings

```dart
final agent = Agent('openai');
final embed = await agent.embedQuery('Hello world');
print(embed.embeddings.length); // 1536
```

## Next

- [Examples](https://github.com/davidmigloz/langchain_dart/tree/main/packages/langchain_compat/example)
- [Providers](/providers)
- [Tools](/tool-calling)
